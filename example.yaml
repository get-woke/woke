ignore_files:
  - README.md
  - pkg/rule/default.go

rules:
  - name: whitelist
    regexp: \b(white-?list)\b
    alternatives: allowlist
    # severity: info

  - name: blacklist
    regexp: \b(black-?list)\b
    alternatives: denylist,blocklist
